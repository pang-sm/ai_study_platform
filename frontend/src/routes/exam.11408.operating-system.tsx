import { createFileRoute } from '@tanstack/react-router';
import { OperatingSystemPage } from '@/features/exam/exam-slice';
export const Route = createFileRoute('/exam/11408/operating-system')({ component: OperatingSystemPage });
