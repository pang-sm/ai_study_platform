# Public build-time configuration only.
# NEVER put API keys, tokens, or any server secret in frontend env files —
# these values are bundled into the client and shipped to the browser.

VITE_API_BASE_URL=http://localhost:8000
