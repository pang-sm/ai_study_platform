# 智学AI — AI Provider Secrets Manifest（EXAMPLE，绝无真实 value）
# 供 STEP12 生产部署参考；生产只需部署真正进入 production pool 的 provider key。

# 变量名                  Provider                        必需/可选      用途
DEEPSEEK_API_KEY         DeepSeek（官方直连）              required      文本生成（primary）
DASHSCOPE_API_KEY        Alibaba Model Studio / Qwen       optional      cross-provider fallback / 文本
ARK_API_KEY              Volcengine Ark / Doubao           optional      待 qualified 后启用
MOONSHOT_API_KEY         Moonshot / Kimi                   optional      待 qualified 后启用
ZHIPUAI_API_KEY          Zhipu / GLM                       optional      待 qualified 后启用
MINIMAX_API_KEY          MiniMax                           optional      待 qualified 后启用

# 生产建议（STEP12 再执行）：
#   - DEV API KEY != PRODUCTION API KEY（若 provider 支持分环境）
#   - 建议开启 IP 白名单 / spending cap / rate cap
#   - 建议经 systemd EnvironmentFile 或 GitHub Actions secrets 注入，不落盘明文到仓库
