# 四语言编程题跨语言重合审计

审计范围：启用且 approved 的 240 道题；本审计只读，不修改数据库。

## 结论

- 判定为跨语言重合的题对：33 对。
- 共享题族：6 个；允许保留：6 个。
- C：60 道；当前重合候选 6 道；计划保留共享 6 道；计划替换 54 道；语言特性候选 56 道。
- C++：60 道；当前重合候选 6 道；计划保留共享 6 道；计划替换 54 道；语言特性候选 60 道。
- Python：60 道；当前重合候选 6 道；计划保留共享 6 道；计划替换 54 道；语言特性候选 56 道。
- Java：60 道；当前重合候选 5 道；计划保留共享 5 道；计划替换 55 道；语言特性候选 60 道。

## 共享题族

- `catalog240-bfs-0`：涉及 4 道跨语言成员；保留。
- `catalog240-brackets-0`：涉及 4 道跨语言成员；保留。
- `catalog240-checksum-0`：涉及 3 道跨语言成员；保留。
- `catalog240-coins-0`：涉及 4 道跨语言成员；保留。
- `catalog240-unique-0`：涉及 4 道跨语言成员；保留。
- `catalog240-words-0`：涉及 4 道跨语言成员；保留。

## 处理规则

- `keep` 仅表示进入六个共享基础题族名额，不代表已经完成最终质量审计。
- `replace` 表示必须设计新任务、稳定 source_key、真实参考解、公开/隐藏测试并重新验证。
- 逐题详细字段和题对指标见 `cross-language-overlap-matrix.json`。
