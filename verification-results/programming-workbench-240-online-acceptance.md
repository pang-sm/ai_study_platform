# 240 题线上部署验收

## 部署

- commit：`38f0805`
- Actions：`30728230170`，结论 `success`
- 正式站点：<http://101.32.190.42/>
- 前端入口 HTTP 200，`/api/health` HTTP 200，响应 `{"status":"ok"}`。

## 线上 API

真实线上 API 返回：C 60、C++ 60、Python 60、Java 60，总计 240。四种语言列表均 HTTP 200，第一页 page size 48；每种语言抽查 5 道详情，标题、摘要、中文题干、输入格式、输出格式、约束均存在，每题 3 个公开样例。详情响应未出现 hidden tests、hidden cases、reference solution 或 reference files。

## 隐藏测试隔离

详情 API 未泄漏隐藏测试：0。提交 API 未执行，因为当前环境没有正式站点登录态；不能用未认证请求冒充提交验收。

## 网页抽验

正式站点页面可以打开并显示 `AI 学习平台`，但当前浏览器没有可用登录态。点击题库入口时页面请求超时，直接对四个验收账号调用 start API 均返回 401。因此登录后的 Workbench 20 道网页抽验、提交结果弹窗、线上终端和页面横向溢出检查均标记为未完成，不宣称网页端通过。

页面加载期间记录到宿主 Statsig telemetry 超时；独立线上 API 核验均正常，未观察到站点 4xx/5xx。

## 未完成项

1. 需要正式站点有效登录态后，补做四种语言各 5 道 Workbench 网页抽验。
2. 需要 SSH 或可见 Actions 部署日志后，补录线上数据库备份绝对路径与大小。
